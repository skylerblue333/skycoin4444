name: SkyCatalog CI

on:
  pull_request:
    branches: [main]
    paths:
      - "packages/sky-catalog/**"
      - "packages/sky-inventory/**"
      - "tsconfig.packages.json"
      - ".github/workflows/sky-catalog.yml"
  push:
    branches: [main]
    paths:
      - "packages/sky-catalog/**"
      - "packages/sky-inventory/**"
      - "tsconfig.packages.json"

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 11.20.0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - name: Strict package typecheck
        run: pnpm run check:packages
      - name: Deterministic catalog and inventory-contract tests
        run: pnpm exec vitest run --config packages/sky-catalog/vitest.config.ts
      - name: Formatting gate
        run: |
          pnpm exec prettier --write packages/sky-catalog
          git diff --exit-code -- packages/sky-catalog
      - name: Production dependency audit
        run: pnpm audit --prod --audit-level=critical

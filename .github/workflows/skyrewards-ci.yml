name: SkyRewards CI

on:
  pull_request:
    branches: [main]
    paths:
      - "server/features/rewards/**"
      - "docs/products/80-skyrewards.md"
      - ".github/workflows/skyrewards-ci.yml"

permissions:
  contents: read

jobs:
  skyrewards-validate:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 11.20.0
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - name: Install locked dependencies
        run: pnpm install --frozen-lockfile
      - name: Format check
        run: pnpm exec prettier --check server/features/rewards/skyRewards.ts server/features/rewards/skyRewards.test.ts docs/products/80-skyrewards.md .github/workflows/skyrewards-ci.yml
      - name: Targeted tests
        run: pnpm exec vitest run server/features/rewards/skyRewards.test.ts
      - name: Typecheck
        run: pnpm run check
      - name: Production build
        run: pnpm run build
      - name: Production dependency audit
        run: pnpm audit --prod --audit-level=critical

name: sky-presence-ci

on:
  pull_request:
    paths:
      - "packages/sky-presence/**"
      - ".github/workflows/sky-presence-ci.yml"
      - "pnpm-lock.yaml"
      - "tsconfig.packages.json"

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 11.20.0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm --filter @skycoin/sky-presence test
      - run: pnpm run check:packages
      - name: Diagnose Prettier diff
        run: |
          pnpm exec prettier --write packages/sky-presence
          git diff -- packages/sky-presence
          git diff --quiet -- packages/sky-presence
      - run: pnpm audit --audit-level high

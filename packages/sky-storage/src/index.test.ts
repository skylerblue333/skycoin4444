import {describe,expect,it} from "vitest";import {createGetPlan,createPutPlan,toStorageIntegrationEvent,validateRef} from "./index";
const ref={bucket:"sky-assets",key:"users/123/avatar.png"};const sha="a".repeat(64);
describe("SkyStorage",()=>{it("creates deterministic provider-neutral put plans",()=>{expect(createPutPlan({ref,contentType:"image/png",sizeBytes:1024,checksumSha256:sha,metadata:{z:"last",a:"first"}})).toEqual({op:"put",ref,contentType:"image/png",sizeBytes:1024,checksumSha256:sha,metadata:{a:"first",z:"last"}})});it("rejects traversal and malformed refs",()=>{expect(()=>validateRef({bucket:"sky-assets",key:"../secret"})).toThrow();expect(()=>validateRef({bucket:"X",key:"ok"})).toThrow()});it("builds get plans without contacting a provider",()=>{expect(createGetPlan(ref)).toEqual({op:"get",ref})});it("emits a bounded integration event",()=>{expect(toStorageIntegrationEvent(ref,"put")).toEqual({type:"storage.object_put",bucket:"sky-assets",key:"users/123/avatar.png",version:undefined})})});

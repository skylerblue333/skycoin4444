import {describe,expect,it} from "vitest"; import {buildFeedPage,toFeedIntegrationEvent} from "./index";
const base=[{id:"post_001",actorId:"user_001",kind:"post" as const,createdAt:"2026-08-25T09:00:00Z",visibility:"public" as const,rank:1},{id:"post_002",actorId:"user_002",kind:"share" as const,createdAt:"2026-08-25T10:00:00Z",visibility:"followers" as const,rank:5},{id:"post_003",actorId:"user_003",kind:"post" as const,createdAt:"2026-08-25T11:00:00Z",visibility:"private" as const,rank:99}];
describe("SkyFeed",()=>{it("orders bounded visible items deterministically",()=>{expect(buildFeedPage(base,10).items.map(i=>i.id)).toEqual(["post_002","post_001"])});it("paginates with opaque item cursors",()=>{const first=buildFeedPage(base,1);expect(first.nextCursor).toBe("post_002");expect(buildFeedPage(base,1,first.nextCursor).items[0]?.id).toBe("post_001")});it("rejects malformed inputs",()=>{expect(()=>buildFeedPage([{...base[0],id:"x"}],10)).toThrow();expect(()=>buildFeedPage(base,0)).toThrow()});it("emits a social integration contract",()=>{expect(toFeedIntegrationEvent(base[0])).toEqual({type:"feed.item_published",itemId:"post_001",actorId:"user_001",kind:"post",visibility:"public"})})});

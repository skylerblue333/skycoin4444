import {describe,expect,it} from "vitest";import {buildSkillIndex,normalizeSkill,resolveSkills,toSkillsIntegrationEvent} from "./index";
describe("SkySkills",()=>{const skills=[{id:"skill_ts",name:"TypeScript",aliases:["TS"],category:"software"},{id:"skill_cn",name:"Mandarin",aliases:["Chinese"],category:"language"}];it("normalizes and resolves taxonomy aliases",()=>{const index=buildSkillIndex(skills);expect(resolveSkills("TS, Chinese",index)).toEqual(["skill_cn","skill_ts"])});it("rejects ambiguous aliases",()=>{expect(()=>buildSkillIndex([...skills,{id:"skill_alt",name:"TS",aliases:[],category:"software"}])).toThrow()});it("deduplicates aliases and profile skills",()=>{expect(normalizeSkill({...skills[0],aliases:["TS","TS"]}).aliases).toEqual(["TS"]);expect(toSkillsIntegrationEvent({subjectId:"user_123",skillIds:["skill_ts","skill_ts"]}).skillIds).toEqual(["skill_ts"])});it("bounds untrusted query size",()=>{expect(()=>resolveSkills("x".repeat(501),buildSkillIndex(skills))).toThrow()})});

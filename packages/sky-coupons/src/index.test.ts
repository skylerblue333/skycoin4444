import {describe,expect,it} from "vitest";import {quoteCoupon,toCouponIntegrationEvent,validateCoupon} from "./index";
const coupon={code:"SAVE10",discount:{type:"percent" as const,basisPoints:1000},active:true,maxRedemptions:2};
describe("SkyCoupons",()=>{it("quotes percent discounts deterministically",()=>{expect(quoteCoupon(coupon,{subtotalMinor:2500,currency:"USD",now:"2026-08-25T10:00:00Z",priorRedemptions:0})).toEqual({code:"SAVE10",eligible:true,discountMinor:250,totalMinor:2250,currency:"USD"})});it("enforces redemption limits and currency",()=>{expect(quoteCoupon(coupon,{subtotalMinor:2500,currency:"USD",now:"2026-08-25T10:00:00Z",priorRedemptions:2}).eligible).toBe(false);expect(quoteCoupon({code:"FIVE",discount:{type:"fixed",amountMinor:500,currency:"USD"},active:true},{subtotalMinor:1000,currency:"EUR",now:"2026-08-25T10:00:00Z",priorRedemptions:0}).eligible).toBe(false)});it("rejects invalid discounts and windows",()=>{expect(()=>validateCoupon({code:"x",discount:{type:"percent",basisPoints:0},active:true})).toThrow()});it("emits a checkout integration event",()=>{expect(toCouponIntegrationEvent("SAVE10",250,"USD").type).toBe("coupon.applied")})});

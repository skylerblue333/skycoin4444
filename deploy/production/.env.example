# Copy to the approved production secret store or host environment; never commit values.
NODE_ENV=production
PORT=3000
VITE_APP_ID=
JWT_SECRET=
OAUTH_SERVER_URL=
OWNER_OPEN_ID=
WEBHOOK_SECRET=
DATABASE_URL=mysql://USER:PASSWORD@HOST:3306/DATABASE
BUILT_IN_FORGE_API_URL=
BUILT_IN_FORGE_API_KEY=
VITE_OAUTH_PORTAL_URL=
VITE_FRONTEND_FORGE_API_URL=
VITE_FRONTEND_FORGE_API_KEY=
# Production deployment policy:
# - Configure the exact OAuth callback URL for the chosen canonical-domain strategy.
# - Keep server-only values out of browser bundles.
# - Do not place real secrets in this file or in Git.

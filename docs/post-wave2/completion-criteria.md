# Completion criteria

A hardening branch is complete only when its scoped change is committed, reviewed through a PR, required CI is successful on the submitted head, and the result is merged or explicitly documented as blocked. Branch creation alone is not counted as completion.

# Batch A completion

A branch is complete only after its scoped change is committed, represented by a PR, exact-head CI is successful, and merge/default-branch inclusion is verified. Merely creating a branch is not completion.

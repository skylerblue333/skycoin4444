# Batch A runbook

Create each hardening branch from current main, implement one narrowly scoped improvement, add focused verification, open a PR, require exact-head CI success, merge, and record the non-counting result.

# Authorization boundary audit

Scope: identify Wave-2 APIs or domain cores that rely on callers for authentication, authorization, tenant isolation, or resource-level access checks.

Acceptance criteria:
- inventory caller-enforced authorization boundaries;
- flag paths that could be mistaken for enforcement;
- verify fail-closed validation where implemented;
- define targeted tests for sensitive transitions;
- retain truthful engineering-beta limitations.

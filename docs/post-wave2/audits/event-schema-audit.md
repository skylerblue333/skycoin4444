# Event schema audit

Scope: review Wave-2 event contracts for stable naming, explicit versions, deterministic serialization assumptions, required-field validation, and forward-compatible boundaries.

Acceptance criteria:
- catalog event identifiers;
- detect naming/version collisions;
- identify optional/required-field ambiguity;
- define regression checks for representative schemas;
- make no new product-count claims.

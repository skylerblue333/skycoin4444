import UnavailableFeature from "@/components/UnavailableFeature";

export default function AITrading() {
  return (
    <UnavailableFeature
      name="AI trading engine"
      reason="Verified model evaluation, live market data, risk controls, order execution, and audited performance reporting are not connected to a production integration."
    />
  );
}

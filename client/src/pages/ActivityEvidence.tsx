/*
 * Activity evidence beta boundary: account-owned persisted events only.
 * This is not an analytics, popularity, revenue, or blockchain activity feed.
 */
import { Activity, ArrowLeft, CheckCircle2, Clock3, ShieldCheck } from "lucide-react";
import { Link } from "wouter";
import { trpc } from "@/lib/trpc";
import { useAuth } from "@/_core/hooks/useAuth";
import { Badge } from "@/components/ui/badge";
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";

export default function ActivityEvidence() {
  const { user } = useAuth();
  const events = trpc.activityEvidence.list.useQuery(undefined, { enabled: !!user });
  return <main className="min-h-screen bg-[#050510] text-white"><header className="border-b border-white/10 bg-[#050510]/95"><div className="mx-auto flex max-w-6xl items-center gap-3 px-4 py-5"><Link href="/beta-workspace" className="text-white/45 hover:text-white"><ArrowLeft className="h-4 w-4" /></Link><div><div className="flex items-center gap-2"><h1 className="font-black">Activity Evidence</h1><Badge variant="outline" className="border-lime-400/40 text-lime-200">Beta</Badge></div><p className="mt-1 text-xs text-white/40">A bounded record of your own persisted product activity</p></div></div></header><div className="mx-auto max-w-6xl space-y-8 px-4 py-10"><section className="grid gap-6 lg:grid-cols-[1.3fr_0.7fr]"><div><p className="text-xs font-semibold uppercase tracking-[0.24em] text-lime-200/70">Evidence, not analytics</p><h2 className="mt-3 text-4xl font-black tracking-tight">See what your account has actually recorded.</h2><p className="mt-4 max-w-2xl text-base leading-7 text-white/55">This view combines bounded records from your posts, lesson progress, feedback, discovery actions, and creator briefs. It does not estimate audience size, engagement, uptime, revenue, rankings, or blockchain activity.</p></div><Card className="border-lime-400/25 bg-lime-400/[0.05] text-white"><CardHeader><ShieldCheck className="h-5 w-5 text-lime-200" /><CardTitle className="mt-3 text-lime-100">Scope boundary</CardTitle><CardDescription className="text-white/55">Only account-owned persisted events are returned, capped to the latest 50.</CardDescription></CardHeader><CardContent className="space-y-2 text-sm text-white/60"><p>No public activity claims</p><p>No inferred metrics</p><p>No wallet or chain events</p></CardContent></Card></section>{!user ? <Card className="border-amber-400/25 bg-amber-400/[0.05] text-white"><CardHeader><CardTitle>Sign in to inspect your activity evidence</CardTitle><CardDescription className="text-white/55">Anonymous activity is intentionally not collected or displayed.</CardDescription></CardHeader></Card> : <Card className="border-white/10 bg-white/[0.03]"><CardHeader><div className="flex items-center justify-between gap-4"><div><CardTitle className="flex items-center gap-2"><Activity className="h-5 w-5 text-lime-200" />Your recorded events</CardTitle><CardDescription className="text-white/50">{events.data?.length ?? 0} events currently available to this account</CardDescription></div><Badge variant="outline" className="border-white/15 text-white/45">Capped at 50</Badge></div></CardHeader><CardContent className="space-y-3">{events.isLoading ? <p className="text-sm text-white/45">Loading account evidence…</p> : events.error ? <p className="text-sm text-rose-200">Activity unavailable: {events.error.message}</p> : (events.data?.length ?? 0) === 0 ? <div className="rounded-xl border border-dashed border-white/15 p-10 text-center"><CheckCircle2 className="mx-auto h-6 w-6 text-white/25" /><p className="mt-3 text-sm text-white/45">No persisted activity evidence is available yet.</p><p className="mt-1 text-xs text-white/30">Complete a lesson, submit feedback, publish a post, save a discovery reference, or create a creator brief.</p></div> : events.data?.map(event => <div key={event.id} className="flex items-start gap-4 rounded-xl border border-white/10 bg-black/20 p-4"><div className="mt-0.5 rounded-lg bg-lime-400/10 p-2 text-lime-200"><Clock3 className="h-4 w-4" /></div><div className="min-w-0"><div className="font-semibold text-white">{event.label}</div><p className="mt-1 text-sm leading-6 text-white/55">{event.detail}</p><p className="mt-2 text-xs text-white/30">{new Date(event.createdAt).toLocaleString()}</p></div></div>)}</CardContent></Card>}<section className="rounded-2xl border border-white/10 bg-white/[0.03] p-5 text-sm leading-6 text-white/55"><ShieldCheck className="mr-2 inline h-4 w-4 text-emerald-300" />This page is an evidence log, not a performance dashboard. It does not authorize or report payments, wallet actions, transfers, signing, settlement, or production-chain execution.</section></div></main>;
}

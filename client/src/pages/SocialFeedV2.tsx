// Legacy route kept for backwards-compatible links.
// The production-shaped social beta surface lives in ActivityFeed and uses
// persisted records plus authenticated write actions instead of fixture posts.
export { default } from "./ActivityFeed";

import { Activity, ArrowRight, CheckCircle2, ExternalLink, LockKeyhole, ShieldAlert } from "lucide-react";
import { Link } from "wouter";
import { Badge } from "@/components/ui/badge";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { routeHealthCounts, routeHealthRegistry, type RouteHealthState } from "@/lib/routeHealth";

const stateCopy: Record<RouteHealthState, { label: string; className: string; icon: typeof CheckCircle2 }> = {
  working: { label: "Working beta", className: "border-emerald-300/25 bg-emerald-300/10 text-emerald-100", icon: CheckCircle2 },
  controlled: { label: "Controlled beta", className: "border-amber-300/25 bg-amber-300/10 text-amber-100", icon: LockKeyhole },
  recovery: { label: "Recovery path", className: "border-rose-300/25 bg-rose-300/10 text-rose-100", icon: ShieldAlert },
};

export default function RouteHealth() {
  return <main className="min-h-screen bg-[#050510] text-white"><header className="border-b border-white/10 bg-[#050510]/95"><div className="mx-auto flex max-w-7xl items-center justify-between gap-4 px-4 py-6"><div><p className="text-xs font-bold uppercase tracking-[0.2em] text-cyan-200/70">Engineering control room</p><h1 className="mt-2 text-3xl font-black tracking-tight">Route Health</h1><p className="mt-2 text-sm text-white/45">One evidence view for the primary product areas and their safest recovery paths.</p></div><Link href="/beta-workspace" className="hidden rounded-xl border border-white/10 bg-white/[0.04] px-4 py-2 text-sm font-semibold text-white/70 hover:bg-white/10 sm:block">Back to workspace</Link></div></header><div className="mx-auto max-w-7xl space-y-8 px-4 py-10"><section className="grid gap-4 sm:grid-cols-3">{([['working','Working',routeHealthCounts.working],['controlled','Controlled',routeHealthCounts.controlled],['recovery','Recovery',routeHealthCounts.recovery]] as const).map(([state,label,count]) => { const Icon = stateCopy[state].icon; return <Card key={state} className="border-white/10 bg-white/[0.03] text-white"><CardContent className="flex items-center justify-between p-5"><div><p className="text-xs uppercase tracking-widest text-white/35">{label}</p><p className="mt-2 text-3xl font-black">{count}</p></div><Icon className="h-6 w-6 text-cyan-200" /></CardContent></Card>; })}</section><Card className="border-cyan-300/15 bg-cyan-300/[0.04] text-white"><CardContent className="flex gap-3 p-5"><Activity className="mt-1 h-5 w-5 shrink-0 text-cyan-200" /><div><p className="font-bold">How to read this view</p><p className="mt-1 text-sm leading-6 text-white/55">Working means the route is registered and its primary local/account-aware journey is available. Controlled means the route intentionally stops short of custody, settlement, provider, or production-scale claims. This registry does not invent uptime, traffic, users, balances, or successful outcomes.</p></div></CardContent></Card><div className="grid gap-4 md:grid-cols-2">{routeHealthRegistry.map(entry => { const meta = stateCopy[entry.state]; const Icon = meta.icon; return <Card key={entry.id} className="border-white/10 bg-white/[0.03] text-white"><CardHeader className="pb-3"><div className="flex items-start justify-between gap-3"><div><CardTitle className="text-lg">{entry.area}</CardTitle><p className="mt-1 font-mono text-xs text-white/35">{entry.route}</p></div><Badge variant="outline" className={meta.className}><Icon className="mr-1 h-3.5 w-3.5" />{meta.label}</Badge></div></CardHeader><CardContent><p className="text-sm leading-6 text-white/55">{entry.evidence}</p><div className="mt-4 flex flex-wrap gap-2"><Link href={entry.route} className="inline-flex items-center rounded-xl bg-white px-3 py-2 text-xs font-bold text-[#050510] hover:bg-white/90">Open screen <ExternalLink className="ml-1.5 h-3.5 w-3.5" /></Link><Link href={entry.recoveryRoute} className="inline-flex items-center rounded-xl border border-white/10 bg-white/[0.04] px-3 py-2 text-xs font-semibold text-white/70 hover:bg-white/10">Recovery path <ArrowRight className="ml-1.5 h-3.5 w-3.5" /></Link></div></CardContent></Card>; })}</div></div></main>;
}

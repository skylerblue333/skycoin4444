import UnavailableFeature from "@/components/UnavailableFeature";

export default function VideoArea() {
  return (
    <UnavailableFeature
      name="Video Area"
      reason="The former route used static reels and videos, fabricated engagement metrics, placeholder media URLs, crypto and yield-farming claims, and local-only upload, play, and stream success states. It is gated until media storage, playback, moderation, analytics, and truthful financial-content sourcing are implemented."
    />
  );
}

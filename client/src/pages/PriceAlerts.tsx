import UnavailableFeature from "@/components/UnavailableFeature";

export default function PriceAlertsPage() {
  return (
    <UnavailableFeature
      name="Price alerts"
      reason="Live market feeds, alert persistence, delivery, and notification metrics are not connected to a verified production integration. This placeholder page is gated instead of claiming real-time alerts or platform statistics."
    />
  );
}

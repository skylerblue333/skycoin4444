import UnavailableFeature from "@/components/UnavailableFeature";

export default function StakingPortal() {
  return (
    <UnavailableFeature
      name="SKY444 staking portal"
      reason="Verified staking custody, APY history, reward calculations, lock periods, and claim transactions are not connected to a production blockchain or audited backend integration."
    />
  );
}

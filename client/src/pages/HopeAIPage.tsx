import UnavailableFeature from "@/components/UnavailableFeature";

export default function HopeAIPage() {
  return (
    <UnavailableFeature
      name="Hope AI"
      reason="Verified model-provider connectivity, conversational responses, content generation, investment or market analysis, anomaly detection, automated support, and AI mining/trading capabilities are not connected to production integrations. The former marketing page implied these capabilities without executable evidence, so it is gated until the AI service contract and failure states are verified."
    />
  );
}

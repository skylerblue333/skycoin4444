import UnavailableFeature from "@/components/UnavailableFeature";

export default function LearningPage() {
  return (
    <UnavailableFeature
      name="Legacy learning page"
      reason="This duplicate Wave 3 page depends on the retired wave3Learning namespace and does not provide a verified course enrollment, progress, completion, or certificate contract. Use only education surfaces with an explicitly connected backend."
    />
  );
}

import UnavailableFeature from "@/components/UnavailableFeature";

export default function AiCorePage() {
  return (
    <UnavailableFeature
      name="Legacy AI Core page"
      reason="This duplicate Wave 2 page depends on the retired wave2AiCore namespace and does not provide a verified contract for chat, content generation, market scanning, learning, or token accounting. Use the supported AI surfaces instead."
    />
  );
}

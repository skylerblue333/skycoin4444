import UnavailableFeature from "@/components/UnavailableFeature";

export default function WorldBrain() {
  return (
    <UnavailableFeature
      name="World Brain"
      reason="The former route used synthetic action previews, confidence scores, market signals, staking APY/yield, engagement predictions, and success toasts without verified AI, market-data, wallet, or execution integrations. It is gated until every action has authenticated validation, real persistence, and truthful failure handling."
    />
  );
}

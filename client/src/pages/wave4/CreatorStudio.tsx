import UnavailableFeature from "@/components/UnavailableFeature";

export default function CreatorStudioPage() {
  return (
    <UnavailableFeature
      name="Legacy creator studio"
      reason="This duplicate Wave 4 page depends on the retired wave4Creator namespace and does not provide a verified publishing, analytics, listing, or payout contract. Use the supported creator surfaces for capabilities that are explicitly connected."
    />
  );
}

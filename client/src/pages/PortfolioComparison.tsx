import UnavailableFeature from "@/components/UnavailableFeature";

export default function PortfolioComparisonPage() {
  return (
    <UnavailableFeature
      name="Portfolio comparison"
      reason="Benchmark returns, holdings, valuations, risk metrics, allocation differences, transaction statistics, and live performance updates are not connected to verified portfolio and market-data providers. This placeholder is gated so it cannot imply investment analytics or fabricated platform metrics."
    />
  );
}

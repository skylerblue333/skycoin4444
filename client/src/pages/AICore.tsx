import UnavailableFeature from "@/components/UnavailableFeature";

export default function AICore() {
  return (
    <UnavailableFeature
      name="AI Core workspace"
      reason="The current AI Core page uses stale generation, usage, and market-analysis procedures that are not exposed as verified production contracts."
    />
  );
}

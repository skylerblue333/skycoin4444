import UnavailableFeature from "@/components/UnavailableFeature";

export default function UnifiedFeed() {
  return (
    <UnavailableFeature
      name="Unified Feed"
      reason="The former feed used static posts and fabricated live streams, AI-trading accuracy, token growth, ICO allocation, NFT pricing, security alerts, and creator revenue claims. A production feed requires authenticated persistence, moderation, verified commerce and financial sources, and truthful event handling, so this route is gated until those integrations are verified."
    />
  );
}
